{
  "name": "comparemarketcapof",
  "private": true,
  "packageManager": "pnpm@12.4.1",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --filter @cmc/web build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "check": "pnpm lint && pnpm typecheck && pnpm test",
    "deploy": "pnpm --filter @cmc/cron deploy && pnpm --filter @cmc/web deploy"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
